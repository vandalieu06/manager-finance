# Documentación legacy

Esta carpeta conserva versiones antiguas o parciales de la documentación.

La documentación principal vigente está organizada en las carpetas `00.indice/` a `12.anexos/` de `docs/`.

La carpeta `final/` conserva los copies finales planos para memoria o Figma.
